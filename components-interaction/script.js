(function declareNamespaceForWitInFive(){

	// this simply declares a variable to use in the global namespace
	// with newer techniques such as requirejs and modules this wouldn't be required

	var wif = window.wif || (window.wif = {});

})();